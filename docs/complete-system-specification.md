# 4DX@HOME システム完成版仕様書

## 📋 概要

4DX@HOME は、家庭で映画館レベルの4D体験を提供するシステムです。
Webアプリケーションから動画を再生し、家庭用デバイスで同期した4D効果（振動・モーション・香り・音響）を体験できます。

## 🎯 システム全体フロー

```
1️⃣ デバイス登録画面
    ↓ (製品コード入力・認証)
2️⃣ 動画選択画面  
    ↓ (demo1.mp4選択)
3️⃣ 待機画面・準備処理
    ↓ (双方向準備完了)
4️⃣ 動画再生・リアルタイム同期
```

## 📱 画面仕様

### 1️⃣ デバイス登録画面

**目的**: システムへの初回アクセス時のデバイス認証

**UI仕様**:
```
┌─────────────────────────┐
│     4DX@HOME System     │
├─────────────────────────┤
│ デバイス製品コード入力:  │
│ [____________] 📝       │
│                        │
│      [登録する] 🔐      │
└─────────────────────────┘
```

**機能要件**:
- 製品コード入力フィールド（例: "DH001", "DH002"）
- リアルタイムバリデーション
- サーバー側認証処理
- 認証成功時の自動画面遷移

**バリデーションルール**:
- 形式: 英数字2-3文字 + 数字3桁
- 例: "DH001", "DX123", "AB999"

### 2️⃣ 動画選択画面

**目的**: 利用可能な4DX対応動画からの選択

**UI仕様**:
```
┌─────────────────────────┐
│    利用可能な4DX動画     │
├─────────────────────────┤
│ 🎬 demo1.mp4           │
│    アクション映画       │
│    [選択] 📽️          │
├─────────────────────────┤
│ 🎬 demo2.mp4 (準備中)   │
│    ホラー映画           │
│    [準備中] ⏳         │
└─────────────────────────┘
```

**機能要件**:
- 動画一覧の動的表示
- サムネイル・メタデータ表示
- 利用可能状況の表示
- 選択時の待機画面遷移

### 3️⃣ 待機画面・準備処理

**目的**: 動画とデバイスの準備状況管理

**UI仕様**:
```
┌─────────────────────────┐
│      準備中です...      │
├─────────────────────────┤
│ 📹 動画準備: [████████] │ 
│ 🏠 デバイス: [██████  ] │
│ 📡 通信確認: [████    ] │
│                        │
│     [再生開始] 🎬      │
│    (準備完了後有効)     │
└─────────────────────────┘
```

**処理フロー**:

**Phase A - 並行準備開始**:
- Webアプリ: 選択動画のプリロード開始
- Cloud Run: デバイスハブに対応JSONファイル送信
- デバイスハブ: JSONファイル受信・解析開始

**Phase B - デバイス通信確認**:
- 振動モーター通信テスト
- モーションアクチュエーター通信テスト
- 香りディフューザー通信テスト
- オーディオシステム通信テスト

**Phase C - 準備完了判定**:
- 動画プリロード完了: 100%
- JSON配信・解析完了: 100% 
- 全アクチュエーター通信確認: 100%
- → 再生ボタン有効化

### 4️⃣ 動画再生・リアルタイム同期

**目的**: 4D効果付き動画体験の提供

**UI仕様**:
```
┌─────────────────────────┐
│                        │
│    🎬 動画再生エリア    │
│                        │
├─────────────────────────┤
│ ▶️ ⏸️ ⏹️  [██████    ] │
│ 00:03:45 / 00:12:30    │
└─────────────────────────┘
```

**同期仕様**:
- 送信間隔: 0.1秒
- データフロー: Webアプリ → Cloud Run → デバイスハブ
- 同期データ: 再生時刻、再生状態、再生速度

## 🔄 API仕様

### デバイス登録・認証
```http
POST /api/device/register
Content-Type: application/json

Request:
{
  "product_code": "DH001"
}

Response:
{
  "device_id": "device_12345",
  "device_name": "4DX Home Basic",
  "capabilities": [
    "VIBRATION", "MOTION", "SCENT", "AUDIO"
  ],
  "status": "registered"
}

Error Response:
{
  "error": "invalid_product_code",
  "message": "製品コードが無効です"
}
```

### 動画一覧取得
```http
GET /api/videos/available?device_id={device_id}

Response:
{
  "videos": [
    {
      "video_id": "demo1",
      "title": "デモ動画1 - アクション",
      "duration": 750,
      "thumbnail_url": "/assets/thumbnails/demo1.jpg",
      "description": "スリリングなアクション映画",
      "ready": true,
      "effects_count": 45
    },
    {
      "video_id": "demo2", 
      "title": "デモ動画2 - ホラー",
      "duration": 680,
      "thumbnail_url": "/assets/thumbnails/demo2.jpg",
      "description": "恐怖のホラー体験",
      "ready": false,
      "effects_count": 38
    }
  ]
}
```

### 動画選択・準備開始
```http
POST /api/videos/select
Content-Type: application/json

Request:
{
  "video_id": "demo1",
  "device_id": "device_12345"
}

Response:
{
  "session_id": "session_abcd1234",
  "video_url": "/assets/videos/demo1.mp4",
  "sync_data_url": "/assets/sync-data/demo1.json",
  "preparation_started": true,
  "estimated_preparation_time": 30
}
```

### 準備状況確認
```http
GET /api/sessions/{session_id}/preparation-status

Response:
{
  "session_id": "session_abcd1234",
  "video_ready": true,
  "video_progress": 100,
  "device_ready": false,
  "sync_data_sent": true,
  "actuator_tests": {
    "vibration": {"status": "ready", "response_time": 120},
    "motion": {"status": "ready", "response_time": 98},
    "scent": {"status": "testing", "response_time": null},
    "audio": {"status": "ready", "response_time": 85}
  },
  "overall_ready": false,
  "preparation_progress": 75
}
```

### 再生開始・時刻同期
```http
POST /api/sessions/{session_id}/playback-sync
Content-Type: application/json

Request:
{
  "video_id": "demo1",
  "current_time": 123.45,
  "playback_rate": 1.0,
  "is_playing": true,
  "timestamp": "2024-10-11T12:34:56.789Z"
}

Response:
{
  "sync_confirmed": true,
  "server_time": "2024-10-11T12:34:56.792Z",
  "latency_ms": 3
}
```

## 📂 ファイル構成

### フロントエンド（実装済み構造）
```
frontend/4dathome-app/
├── public/
│   ├── vite.svg
│   └── assets/
│       ├── movie.mp4           # デモ動画ファイル
│       └── react.svg
├── src/
│   ├── App.tsx                 # メインアプリケーション
│   ├── main.tsx               # エントリーポイント
│   ├── components/
│   │   └── AppHeader.tsx       # 共通ヘッダーコンポーネント
│   ├── pages/
│   │   ├── PairingPage.tsx     # デバイス登録画面（実装済み）
│   │   └── PlayerPage.tsx      # 動画再生画面（実装済み）
│   ├── hooks/
│   │   └── useSessionApi.ts    # セッションAPI管理フック
│   ├── types/
│   │   └── session.ts          # セッション型定義
│   ├── utils/
│   │   ├── apt.ts             # API通信ユーティリティ
│   │   └── socket.ts          # WebSocket通信管理
│   └── assets/
│       └── react.svg
├── package.json               # 依存関係管理
├── vite.config.tsx           # Vite設定
├── tailwind.config.cjs       # TailwindCSS設定
└── tsconfig.json             # TypeScript設定
```

### 必要な追加実装（フロントエンド）
```
src/
├── pages/
│   ├── VideoSelection.tsx      # 動画選択画面（新規追加）
│   └── WaitingScreen.tsx      # 準備待機画面（新規追加）
├── services/
│   ├── DeviceService.ts       # デバイス登録API（新規追加）
│   ├── VideoService.ts        # 動画管理API（新規追加）
│   └── PreparationService.ts  # 準備処理API（新規追加）
└── types/
    ├── device.ts             # デバイス型定義（新規追加）
    ├── video.ts              # 動画型定義（新規追加）
    └── preparation.ts        # 準備処理型定義（新規追加）
```

### バックエンド（Cloud Run）
```
app/
├── api/
│   ├── device_registration.py   # デバイス登録・認証
│   ├── video_management.py      # 動画一覧・選択
│   ├── preparation.py           # 準備処理制御
│   └── phase1.py               # 既存セッション管理（拡張）
├── models/
│   ├── device.py               # デバイスモデル
│   ├── video.py                # 動画モデル
│   └── schemas.py              # 既存スキーマ（拡張）
├── services/
│   ├── device_service.py       # デバイス管理サービス
│   ├── video_service.py        # 動画管理サービス
│   └── preparation_service.py  # 準備処理サービス
└── data/
    ├── devices.json           # 製品コードマスタ
    └── sync-patterns/
        ├── demo1.json         # demo1用効果データ
        └── demo2.json         # demo2用効果データ
```

### デバイスハブ
```
hardware/device-hub/
├── src/
│   ├── hub.py                  # メインハブ（拡張）
│   ├── preparation_handler.py  # 準備処理ハンドラ
│   ├── actuator_tester.py      # アクチュエーターテスト
│   └── sync_processor.py       # 同期データプロセッサ（拡張）
└── config/
    └── actuators.json         # アクチュエーター設定
```

## 🔐 セキュリティ仕様

### デバイス認証
- 製品コードによる初回認証
- セッションベースの継続認証
- デバイスID生成・管理

### 通信セキュリティ
- HTTPS必須（Cloud Run: `https://fourdk-backend-333203798555.asia-northeast1.run.app`）
- CORS適切な設定（フロントエンド・API間通信）
- WebSocket Secure (WSS)（`wss://fourdk-backend-333203798555.asia-northeast1.run.app`）

### データ保護
- 個人情報の最小化
- セッションデータの適切な有効期限
- ログの匿名化

## 📊 パフォーマンス要件

### レスポンス時間
- デバイス登録: < 2秒
- 動画一覧取得: < 1秒
- 準備処理開始: < 3秒
- 同期データ送信: < 500ms

### 同期精度
- 時刻同期誤差: < 100ms
- 効果実行遅延: < 200ms
- 通信断絶時の復旧: < 5秒

### スケーラビリティ
- 同時接続デバイス数: 100台
- 同時再生セッション数: 50セッション
- Cloud Run並行処理: 10インスタンス

## 🧪 テスト要件

### 単体テスト
- 各APIエンドポイントの機能テスト
- バリデーション処理のテスト
- エラーハンドリングのテスト

### 統合テスト
- フロントエンド-バックエンド通信テスト
- バックエンド-デバイスハブ通信テスト
- エンドツーエンド動画再生テスト

### パフォーマンステスト
- 負荷テスト（同時接続）
- レスポンス時間測定
- 長時間動作安定性テスト

## 🚀 デプロイメント

### 環境構成
- **本番**: Google Cloud Run（asia-northeast1）
  - URL: `https://fourdk-backend-333203798555.asia-northeast1.run.app`
  - プロジェクトID: `fourdk-backend-333203798555`
- **ステージング**: Cloud Run（テスト用）
- **開発**: ローカル環境（`http://localhost:8080`）

### CI/CD
- GitHub Actions
- 自動テスト実行
- 自動デプロイメント

---

**更新日**: 2025年10月11日
**バージョン**: 1.0.0
**ステータス**: 仕様策定完了