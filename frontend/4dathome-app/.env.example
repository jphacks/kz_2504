# 4DX@HOME Frontend Environment Variables
# このファイルを .env にコピーして実際のURLを設定してください
# .env ファイルは .gitignore に含まれており、Gitにコミットされません

# バックエンドAPI URL（Cloud Run）
VITE_BACKEND_API_URL=https://your-backend-api.run.app

# バックエンドWebSocket URL（Cloud Run）
VITE_BACKEND_WS_URL=wss://your-backend-api.run.app

# ローカル開発の場合:
# VITE_BACKEND_API_URL=http://localhost:8000
# VITE_BACKEND_WS_URL=ws://localhost:8000
